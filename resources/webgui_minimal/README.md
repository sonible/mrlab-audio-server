# webgui_minimal

This is a minimal webgui example two launch and quit two mrlab apps via buttons.

The communication takes place via OSC over a WebSocket provided by the mrlabctrl server.

This example is likely to be superseded by further development and might be removed again.
